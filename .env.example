# Optional keys. The project runs locally without them.
GEMINI_API_KEY=
OPENAI_API_KEY=
TAVILY_API_KEY=
# Opt in to real locally cached Hugging Face embeddings (otherwise deterministic local fallback).
USE_NEURAL_EMBEDDINGS=0
