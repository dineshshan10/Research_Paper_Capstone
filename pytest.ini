[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    slow: tests that load embedding models or build indices
    requires_key: tests needing a live API key (skipped when absent)
